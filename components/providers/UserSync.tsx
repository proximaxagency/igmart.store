// UserSync is no longer needed — @convex-dev/auth handles user creation automatically
// Kept as empty export for backward compatibility with existing imports
export function UserSync() {
  return null;
}
